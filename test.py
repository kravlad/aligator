from defs import send_telegram
import asyncio



# loop = asyncio.get_event_loop().run_until_complete
# loop(send_telegram('<>gtntmnyjmjy'))

asyncio.run(send_telegram('<code>"<>gtntmnyjmjy"</code>'))
